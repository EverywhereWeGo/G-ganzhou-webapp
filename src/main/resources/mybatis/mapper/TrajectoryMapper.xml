<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.bfd.webappxcdc.c_dao.TrajectoryNowDao">
	<select id="getTrajectnNow" parameterType="java.lang.String" resultType="com.bfd.webappxcdc.vo.TrajectoryNowVO">
        select
        xm as name,
        card_code as id,
        cur_col_time as time,
        cur_latitude as latitude,
        cur_longitude as longitude
        from
        fact_person_position_new
        where
        card_code=#{id}
	</select>

    <select id="isKeyPerson" parameterType="java.lang.String" resultType="java.lang.String">
        select
        xm
        from
        fact_key_person
        where
        sfzh=#{id}
    </select>



    <select id="getChartNum" parameterType="java.lang.String"
            resultType="com.bfd.webappxcdc.vo.PersonThermodynamicChartVO">
        SELECT sfzh as sfzh ,sbbh as sbbh,longitude as longitude,latitude as latitude ,pass_num as passnum FROM model_key_person_hotmap WHERE sfzh=#{hm}
    </select>

</mapper>
